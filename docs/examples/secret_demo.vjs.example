# flow: secret_demo — proves a secret resolves at run time and is never exposed
source "vx.secret.test"

api_key = secret("demo/token")

# we emit PROOF of resolution (length, and whether it authenticated), never the value
emit "vx.audit.log", {authenticated: len(api_key) > 0, key_length: len(api_key), caller: caller}
